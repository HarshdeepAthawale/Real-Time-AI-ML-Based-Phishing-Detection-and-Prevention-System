# Required for Docker Compose
POSTGRES_PASSWORD=change_me_secure_password

# Frontend (build-time, used by browser – must be reachable from user's browser)
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_WS_URL=http://localhost:3000

# Optional: Enable WebSocket events
ENABLE_WEBSOCKET=true

# Optional: Threat intel API keys
# MISP_URL=
# MISP_API_KEY=
# OTX_API_KEY=
# VIRUSTOTAL_API_KEY=
# PHISHTANK_API_KEY=

# Optional: Sandbox (AnyRun/Cuckoo)
# Without these, sandbox service starts in disabled mode
# SANDBOX_PROVIDER=anyrun
# ANYRUN_API_KEY=
# CUCKOO_SANDBOX_URL=
# CUCKOO_API_KEY=

# Smoke / integration tests (seeded by DB init on first run)
# TEST_API_KEY=testkey_smoke_test_12345
